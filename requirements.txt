streamlit>=1.0.0
spacy>=3.8.0  # Just the spacy library, not the specific model data package
pypdf2>=3.0.1
python-docx>=1.1.2
# Add any other dependencies your app might need (e.g., pandas if you use it directly)
# For example, if you need pandas explicitly:
# pandas>=2.2.3
